apiVersion: v1
kind: Service
metadata:
  name: be 
  namespace: to-do
spec:
  selector:
      project: to-do-list
      component: backend
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}